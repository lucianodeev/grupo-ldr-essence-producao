[build]
  command = "npm --prefix apps/painel-ldr run build"
  publish = "apps/painel-ldr/dist/client"
  functions = "apps/painel-ldr/.netlify/v1/functions"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--no-audit --no-fund"

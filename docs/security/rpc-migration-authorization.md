# Authorization

A server-side replacement must enforce authorization equivalent to or stricter than the RPC it replaces. Moving code server-side is not sufficient unless token/user ownership checks remain explicit.
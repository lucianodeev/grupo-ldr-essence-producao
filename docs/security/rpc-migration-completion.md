# Completion criteria

An RPC is considered migrated only when the old anonymous call path is no longer required, the replacement has been validated, the anonymous EXECUTE grant is revoked, and the Supabase Security Advisor confirms the expected warning reduction.
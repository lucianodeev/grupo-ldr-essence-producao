# Compatibility

Do not remove an old RPC endpoint in the same step that introduces its replacement. First deploy and validate the replacement; revoke the old anonymous grant only in a later independently reversible step.
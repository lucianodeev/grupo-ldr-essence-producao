# Deployed consumers

Repository code search does not prove absence of deployed, older, external or dynamically constructed RPC consumers. Validate deployed architecture before revoking a grant based only on source-search absence.
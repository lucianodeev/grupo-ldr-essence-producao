# Minimum flow testing

Validate the exact migrated user flow on both success and authorization-failure paths. For browser-facing changes, include authenticated/session behavior and ensure service credentials never reach the client bundle.
# Legacy classification

Classify an RPC as legacy only when repository/deployment callers, internal database references and product-flow ownership provide sufficient evidence. A zero exact-name search result is only one signal.
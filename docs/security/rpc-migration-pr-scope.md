# Pull request scope

The security PR should remain draft until at least one real replacement path is implemented. Documentation-only commits establish the safety baseline but are not themselves a completed RPC migration.
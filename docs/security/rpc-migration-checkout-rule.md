# Checkout rule

Checkout replacements must preserve sale ownership, pending-session rules, Stripe identifiers and idempotency. Never allow the browser to attach arbitrary checkout sessions to unrelated sales.
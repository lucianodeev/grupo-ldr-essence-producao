# First target

`ldr_simple_logout(uuid)` is the first low-risk discovery target because default-branch repository code search returned no direct reference. This is not sufficient evidence to revoke its grant; deployed/external callers and database dependencies still need validation.
# Review requirements

Before merge: inspect diff, run relevant tests/build, confirm no production database mutation occurred during code-only work, and validate the exact affected user flow before changing database grants.
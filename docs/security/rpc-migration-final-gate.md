# Final gate

A zero-warning target is desirable only after legitimate public custom-token entry points have been replaced. Until then, documented intentional exposure with strong token validation is safer than breaking active application flows.
# Next implementation action

Trace logout/session behavior beyond exact RPC-name search, including generic `.rpc(...)` wrappers and API/server routes. Implement a replacement only after locating the real call path or proving the function is legacy across source and deployed architecture.
# Production safety

Do not merge a grant revocation to production before its replacement path is validated. Discovery and documentation changes are safe to merge only when they do not alter runtime behavior.
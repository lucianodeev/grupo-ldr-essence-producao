import {createFileRoute,Link} from "@tanstack/react-router";import {useEffect,useState} from "react";import {Building2,GraduationCap,BriefcaseBusiness,ShieldCheck} from "lucide-react";import {supabase} from "@/integrations/supabase/client";
export const Route=createFileRoute("/carreira/empresa-escola")({component:Hub});
function Hub(){const[user,setUser]=useState<any>();const[rows,setRows]=useState<any[]>([]);useEffect(()=>{void(async()=>{const{data}=await supabase.auth.getUser();setUser(data.user??null);if(!data.user)return;const{data:r}=await(supabase.from("ldr_experience_projects" as never)as any).select("id,title,description,modality,status,organization_name,compensation_type,compensation_amount,currency,starts_at,ends_at").order("created_at",{ascending:false});setRows(r??[])})()},[]);
const label=(m:string)=>({simulation:"Simulação LDR",educational_challenge:"Desafio educacional",paid_microproject:"Microprojeto profissional",sponsored_project:"Projeto patrocinado"} as any)[m]??m;
if(user===undefined)return null;return <main className="min-h-screen bg-slate-50"><div className="mx-auto max-w-6xl px-5 py-10"><p className="text-xs font-black uppercase tracking-[.18em] text-[#c99b2d]">LDR Empresa-Escola</p><h1 className="mt-2 text-4xl font-black text-[#07345b]">Aprender fazendo</h1><p className="mt-2 max-w-3xl text-slate-600">Experiências com contexto real para praticar, colaborar e construir evidências profissionais.</p><div className="mt-6 flex gap-3 rounded-2xl border border-blue-100 bg-blue-50 p-4 text-sm text-[#07345b]"><ShieldCheck size={20}/><p>Simulações e desafios educacionais são separados de trabalho produtivo. Microprojetos e projetos patrocinados exibem a condição de remuneração registrada no projeto.</p></div>{!user?<a href="/cliente/login?next=%2Fcarreira%2Fempresa-escola" className="mt-8 inline-flex rounded-xl bg-[#07345b] px-5 py-3 font-bold text-white">Entrar</a>:<div className="mt-8 grid gap-5 md:grid-cols-2">{rows.length?rows.map(r=><Link key={r.id} to="/carreira/projetos/$projectId" params={{projectId:r.id}} className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><div className="flex gap-3">{r.modality==="simulation"||r.modality==="educational_challenge"?<GraduationCap className="text-[#c99b2d]"/>:<BriefcaseBusiness className="text-[#c99b2d]"/>}<div><p className="text-xs font-black uppercase text-slate-500">{label(r.modality)}</p><h2 className="text-xl font-black text-[#07345b]">{r.title}</h2></div></div>{r.organization_name&&<p className="mt-2 text-sm text-slate-500">{r.organization_name}</p>}<p className="mt-4 text-sm font-bold text-[#07345b]">{r.compensation_type==="none"?"Experiência educacional":r.compensation_amount!=null?new Intl.NumberFormat("pt-BR",{style:"currency",currency:r.currency??"EUR"}).format(r.compensation_amount):"Condição financeira no projeto"}</p></Link>):<div className="rounded-3xl bg-white p-8 shadow-sm"><Building2 className="text-[#c99b2d]"/><p className="mt-3">Nenhum projeto disponível para sua conta ainda.</p></div>}</div>}<Link to="/carreira/work" className="mt-8 inline-flex font-bold text-[#07345b]">← LDR Work</Link></div></main>}
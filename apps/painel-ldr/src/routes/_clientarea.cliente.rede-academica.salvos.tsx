import { createFileRoute, Link } from "@tanstack/react-router";
import { useQuery } from "@tanstack/react-query";
import { useServerFn } from "@tanstack/react-start";
import { Bookmark, BookOpen } from "lucide-react";
import { academicSavedContent } from "@/lib/academic-saved-v2.functions";
export const Route=createFileRoute("/_clientarea/cliente/rede-academica/salvos")({component:AcademicSavedPage});
function AcademicSavedPage(){const list=useServerFn(academicSavedContent);const {data,isLoading,error}=useQuery({queryKey:["academic-saved-content"],queryFn:()=>list()});return <div className="mx-auto max-w-5xl space-y-5 px-3 pb-28 pt-4 sm:px-6 sm:pt-6"><Link to="/cliente/rede-academica" className="text-xs font-black text-[#07315a] dark:text-[#efc56d]">← REDE ACADÊMICA</Link><header><div className="flex items-center gap-2"><Bookmark className="h-6 w-6 text-[#c99b38]"/><h1 className="font-serif text-3xl">Salvos</h1></div><p className="mt-1 text-sm text-muted-foreground">Somente você vê esta área.</p></header>{isLoading?<p className="text-sm text-muted-foreground">Carregando…</p>:error||!data?<div className="rounded-2xl border p-5">Não foi possível carregar os itens salvos.</div>:<div className="grid gap-6 lg:grid-cols-2"><section><h2 className="mb-3 font-serif text-xl">Publicações</h2><div className="space-y-3">{data.posts.length?data.posts.map((p:any)=><Link key={p.id} to="/cliente/rede-academica/post/$id" params={{id:p.id}} className="block rounded-2xl border bg-card p-4 shadow-sm"><span className="text-[10px] font-black text-[#9a772c]">{String(p.post_type).toUpperCase()}</span><p className="mt-2 line-clamp-4 whitespace-pre-wrap text-sm leading-6">{p.body}</p></Link>):<Empty/>}</div></section><section><h2 className="mb-3 font-serif text-xl">Artigos</h2><div className="space-y-3">{data.articles.length?data.articles.map((a:any)=><Link key={a.id} to="/cliente/rede-academica/artigo/$slug" params={{slug:a.slug}} className="block rounded-2xl border bg-card p-4 shadow-sm"><BookOpen className="h-4 w-4 text-[#c99b38]"/><b className="mt-2 block font-serif text-lg">{a.title}</b><p className="mt-1 line-clamp-3 text-sm text-muted-foreground">{a.summary}</p></Link>):<Empty/>}</div></section></div>}</div>}
function Empty(){return <div className="rounded-2xl border border-dashed p-6 text-center text-sm text-muted-foreground">Nada salvo nesta categoria.</div>}

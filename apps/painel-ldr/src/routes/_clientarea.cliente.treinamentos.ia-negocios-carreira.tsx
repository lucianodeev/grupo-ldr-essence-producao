import { createFileRoute,Link } from "@tanstack/react-router";
import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";
import { useServerFn } from "@tanstack/react-start";
import { useMemo,useState } from "react";
import { AI_BUSINESS_CAREER_MODULES,AI_PROJECTS } from "@/content/ai-business-career-curriculum";
import { clientAIFormationExperience,clientSaveAIFormationState,clientSubmitAIProject } from "@/lib/ai-formation-commerce.functions";
export const Route=createFileRoute("/_clientarea/cliente/treinamentos/ia-negocios-carreira")({component:AIFormation});
function AIFormation(){const expFn=useServerFn(clientAIFormationExperience),saveFn=useServerFn(clientSaveAIFormationState),submitFn=useServerFn(clientSubmitAIProject),qc=useQueryClient();const {data,isLoading,error}=useQuery({queryKey:["ai-formation-experience"],queryFn:()=>expFn({})});const all=useMemo(()=>AI_BUSINESS_CAREER_MODULES.flatMap(m=>m.lessons.map(l=>({...l,module:m}))),[]);const [lessonId,setLessonId]=useState(all[0].id),[tab,setTab]=useState<"curso"|"projetos">("curso"),[projectNumber,setProjectNumber]=useState(1),[title,setTitle]=useState(""),[text,setText]=useState(""),[url,setUrl]=useState("");const completed:string[]=data?.completedLessons??[],current=all.find(x=>x.id===lessonId)??all[0];const save=useMutation({mutationFn:(ids:string[])=>saveFn({data:{state:{...(data?.state??{}),completedLessons:ids,currentLessonId:lessonId}}}),onSuccess:()=>qc.invalidateQueries({queryKey:["ai-formation-experience"]})});const submit=useMutation({mutationFn:()=>submitFn({data:{projectNumber,title,projectText:text,projectUrl:url||null}}),onSuccess:()=>{setTitle("");setText("");setUrl("");qc.invalidateQueries({queryKey:["ai-formation-experience"]})}});if(isLoading)return <p>Carregando formação…</p>;if(error||!data)return <div className="rounded-2xl border p-6"><p>Não foi possível abrir esta formação.</p><Link to="/cliente/biblioteca" className="mt-4 inline-block underline">Voltar à Biblioteca</Link></div>;const pct=Math.round(completed.length/225*100),mark=()=>{if(!completed.includes(current.id))save.mutate([...completed,current.id])};const p=AI_PROJECTS.find(x=>x.number===projectNumber)!;const submission=(data.projectSubmissions??[]).find((s:any)=>s.submission_number===projectNumber);const eligible=completed.length>=p.afterModule*15;return <div className="min-h-screen bg-[#f4f9fc] text-[#071426]"><header className="border-b bg-gradient-to-r from-[#071426] to-[#143d59] text-white"><div className="mx-auto max-w-7xl px-4 py-6"><Link to="/cliente/biblioteca" className="text-xs font-bold text-[#9ed8ff]">← Biblioteca</Link><p className="mt-3 text-[10px] font-black uppercase tracking-[.2em] text-[#9ed8ff]">FORMAÇÃO PROFISSIONAL · 600H</p><h1 className="mt-1 max-w-4xl font-serif text-3xl">Inteligência Artificial Aplicada aos Negócios e à Carreira</h1><p className="mt-2 text-sm text-white/70">15 módulos · 225 aulas · 100% online · 5 projetos + Projeto Final</p><div className="mt-4 h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full bg-[#66c4ff]" style={{width:`${pct}%`}}/></div><p className="mt-1 text-xs">{completed.length}/225 aulas · {pct}%</p></div></header><main className="mx-auto max-w-7xl px-4 py-6"><div className="mb-5 flex gap-2"><button onClick={()=>setTab("curso")} className={`rounded-xl px-4 py-2 text-sm font-black ${tab==="curso"?"bg-[#143d59] text-white":"border bg-white"}`}>FORMAÇÃO</button><button onClick={()=>setTab("projetos")} className={`rounded-xl px-4 py-2 text-sm font-black ${tab==="projetos"?"bg-[#143d59] text-white":"border bg-white"}`}>6 AVALIAÇÕES DE PROJETO</button></div>{tab==="curso"?<div className="grid gap-5 lg:grid-cols-[310px_1fr]"><aside className="max-h-[72vh] overflow-auto rounded-2xl border bg-white p-3">{AI_BUSINESS_CAREER_MODULES.map(m=><div key={m.number} className="mb-5"><p className="px-2 text-[10px] font-black uppercase tracking-[.1em] text-[#0b5cab]">Módulo {m.number} · {m.title}</p>{m.lessons.map(l=><button key={l.id} onClick={()=>setLessonId(l.id)} className={`mt-1 w-full rounded-lg px-2 py-2 text-left text-xs ${lessonId===l.id?"bg-[#e9f5ff] font-bold":"hover:bg-slate-50"}`}>{completed.includes(l.id)?"✓ ":""}{l.number}. {l.title}</button>)}</div>)}</aside><article className="rounded-2xl border bg-white p-5 sm:p-8"><p className="text-xs font-black uppercase tracking-[.15em] text-[#0b5cab]">MÓDULO {current.module.number} · AULA {current.number}</p><h2 className="mt-2 font-serif text-3xl">{current.title}</h2><section className="mt-6 space-y-6 text-[15px] leading-7 text-slate-700">{[["Objetivo",current.objective],["Contexto",current.introduction],["Desenvolvimento",current.development],["Exemplo aplicado",current.example],["Exercício prático",current.exercise],["Entrega para o portfólio",current.action],["Síntese",current.summary]].map(([h,b],i)=><div key={h} className={i===3?"rounded-xl bg-[#eef8ff] p-4":""}><h3 className="font-black text-[#071426]">{h}</h3><p>{b}</p></div>)}</section><button disabled={completed.includes(current.id)||save.isPending} onClick={mark} className="mt-8 w-full rounded-xl bg-[#143d59] px-5 py-3 font-black text-white">{completed.includes(current.id)?"✓ AULA CONCLUÍDA":"MARCAR COMO CONCLUÍDA"}</button></article></div>:<div className="grid gap-5 lg:grid-cols-[330px_1fr]"><aside className="space-y-2">{AI_PROJECTS.map(x=>{const s=(data.projectSubmissions??[]).find((q:any)=>q.submission_number===x.number);return <button key={x.number} onClick={()=>setProjectNumber(x.number)} className={`w-full rounded-xl border p-4 text-left ${projectNumber===x.number?"border-[#0b5cab] bg-[#e9f5ff]":"bg-white"}`}><p className="text-[10px] font-black text-[#0b5cab]">AVALIAÇÃO {x.number} · APÓS MÓDULO {x.afterModule}</p><p className="mt-1 font-bold">{x.title}</p><p className="mt-1 text-xs text-slate-500">{s?`Status: ${s.status}`:"Ainda não enviada"}</p></button>})}</aside><section className="rounded-2xl border bg-white p-6"><p className="text-xs font-black uppercase tracking-[.15em] text-[#0b5cab]">AVALIAÇÃO DE PROJETO {p.number}/6</p><h2 className="mt-2 font-serif text-3xl">{p.title}</h2><p className="mt-3 text-sm leading-6 text-slate-600">{p.deliverable}</p><div className="mt-4 rounded-xl bg-[#f1f7fb] p-4 text-sm"><b>Critérios:</b> clareza do problema, aplicação adequada de IA, qualidade da execução, validação, documentação, ética/segurança e capacidade de justificar decisões. Quando forem solicitados ajustes, o aluno poderá corrigir e reenviar.</div>{submission?.feedback?<div className="mt-4 rounded-xl bg-amber-50 p-4 text-sm"><b>Feedback:</b> {submission.feedback}</div>:null}{eligible&&(!submission||submission.status==="changes_requested")?<div className="mt-5 space-y-3"><input value={title} onChange={e=>setTitle(e.target.value)} className="w-full rounded-xl border p-3" placeholder="Nome do projeto"/><input value={url} onChange={e=>setUrl(e.target.value)} className="w-full rounded-xl border p-3" placeholder="Link do projeto (opcional)"/><textarea value={text} onChange={e=>setText(e.target.value)} rows={12} className="w-full rounded-xl border p-3" placeholder="Documente problema, público, solução, ferramentas, uso de IA, validação, resultados, riscos e próximos passos…"/><button disabled={!title.trim()||!text.trim()||submit.isPending} onClick={()=>submit.mutate()} className="w-full rounded-xl bg-[#143d59] px-5 py-3 font-black text-white">{submission?"REENVIAR COM AJUSTES":"ENVIAR PARA AVALIAÇÃO"}</button>{submit.error?<p className="text-sm text-red-600">{String((submit.error as Error).message)}</p>:null}</div>:!eligible?<div className="mt-5 rounded-xl bg-amber-50 p-4 text-sm font-bold text-amber-800">Esta entrega libera após o módulo {p.afterModule}. Progresso necessário: {p.afterModule*15} aulas; atual: {completed.length}.</div>:<div className="mt-5 rounded-xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800">Projeto enviado. Status: {submission?.status}.</div>}<div className="mt-6 rounded-xl bg-[#071426] p-4 text-sm text-white"><b>Certificação</b><p className="mt-1 text-white/70">Conclusão acadêmica: 225 aulas concluídas e aprovação das 6 avaliações de projeto, incluindo o Projeto Final.</p></div></section></div>}</main></div>}

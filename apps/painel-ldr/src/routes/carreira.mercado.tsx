import {createFileRoute,Link} from "@tanstack/react-router";import {useEffect,useState} from "react";import {Store,UserRound,ShieldCheck} from "lucide-react";import {supabase} from "@/integrations/supabase/client";
export const Route=createFileRoute("/carreira/mercado")({component:Market});
function Market(){const[profiles,setProfiles]=useState<any[]>([]);useEffect(()=>{void(async()=>{const{data:p}=await(supabase.from("professional_profiles" as never)as any).select("id,slug,display_name,professional_title,city,country_code,languages,online_enabled,in_person_enabled,photo_url").eq("is_public",true).eq("profile_status","active").eq("compliance_status","approved").order("display_name").limit(60);setProfiles(p??[])})()},[]);
return <main className="min-h-screen bg-slate-50"><div className="mx-auto max-w-6xl px-5 py-10"><p className="text-xs font-black uppercase tracking-[.18em] text-[#c99b2d]">LDR Mercado</p><h1 className="mt-2 text-4xl font-black text-[#07345b]">Encontre profissionais e serviços</h1><p className="mt-2 max-w-3xl text-slate-600">Descubra perfis profissionais aprovados na rede e continue pelo fluxo oficial de serviço e agendamento já existente.</p><div className="mt-6 flex gap-3 rounded-2xl border border-blue-100 bg-blue-50 p-4 text-sm text-[#07345b]"><ShieldCheck size={20}/><p>Esta área não cria um checkout paralelo: contratação e pagamento continuam no fluxo oficial do perfil profissional.</p></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{profiles.length?profiles.map(p=><Link key={p.id} to={"/profissional/"+p.slug as any} className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><UserRound className="text-[#c99b2d]"/><h2 className="mt-4 text-xl font-black text-[#07345b]">{p.display_name}</h2><p className="mt-1 text-sm font-bold text-slate-700">{p.professional_title}</p><p className="mt-2 text-sm text-slate-500">{[p.city,p.country_code].filter(Boolean).join(" · ")}</p><span className="mt-5 inline-flex font-bold text-[#07345b]">Ver serviços →</span></Link>):<div className="rounded-3xl bg-white p-8 shadow-sm"><Store className="text-[#c99b2d]"/><p className="mt-3">Nenhum perfil público disponível agora.</p></div>}</div><Link to="/carreira/work" className="mt-8 inline-flex font-bold text-[#07345b]">← LDR Work</Link></div></main>}
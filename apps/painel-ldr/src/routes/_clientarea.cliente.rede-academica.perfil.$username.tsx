import { createFileRoute, Link } from "@tanstack/react-router";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { useServerFn } from "@tanstack/react-start";
import { Bookmark, BookOpen, HeartHandshake, MapPin, Share2, UserPlus } from "lucide-react";
import { useState } from "react";
import { academicSocialProfile, academicToggleFollow } from "@/lib/academic-social-v2.functions";

export const Route=createFileRoute("/_clientarea/cliente/rede-academica/perfil/$username")({component:AcademicSocialProfilePage});
type Tab="posts"|"articles"|"reflections"|"photos";
function AcademicSocialProfilePage(){
  const {username}=Route.useParams();const qc=useQueryClient();const getProfile=useServerFn(academicSocialProfile),toggleFollow=useServerFn(academicToggleFollow);const [tab,setTab]=useState<Tab>("posts");
  const {data,isLoading,error}=useQuery({queryKey:["academic-social-profile",username],queryFn:()=>getProfile({data:{username}})});
  const follow=useMutation({mutationFn:(id:string)=>toggleFollow({data:{targetProfileId:id}}),onSuccess:()=>qc.invalidateQueries({queryKey:["academic-social-profile",username]})});
  if(isLoading)return <div className="mx-auto max-w-5xl p-6 text-sm text-muted-foreground">Carregando perfil acadêmico…</div>;
  if(error||!data)return <div className="mx-auto max-w-3xl p-6"><div className="rounded-2xl border p-5">Perfil não disponível.</div></div>;
  const p=data.profile;const share=async()=>{const url=window.location.href;if(navigator.share)await navigator.share({title:`@${p.username} · Rede Acadêmica LDR`,url});else await navigator.clipboard.writeText(url)};
  const tabs:[Tab,string][]=[["posts","PUBLICAÇÕES"],["articles","ARTIGOS"],["reflections","REFLEXÕES"],["photos","FOTOS"]];
  const posts=tab==="reflections"?data.posts.filter((x:any)=>x.post_type==="reflection"):data.posts;
  return <div className="mx-auto max-w-5xl space-y-5 px-3 pb-24 pt-4 sm:px-6 sm:pt-6">
    <Link to="/cliente/rede-academica" className="text-xs font-black text-[#07315a] dark:text-[#efc56d]">← REDE ACADÊMICA</Link>
    <section className="overflow-hidden rounded-[30px] border bg-card shadow-sm"><div className="h-28 bg-gradient-to-r from-[#071426] via-[#0d3156] to-[#195788] sm:h-36"/><div className="px-5 pb-6 sm:px-8"><div className="-mt-10 flex flex-col gap-4 sm:-mt-12 sm:flex-row sm:items-end sm:justify-between"><div className="flex items-end gap-4">{p.avatarUrl?<img src={p.avatarUrl} alt={`Foto de ${p.name}`} className="h-20 w-20 rounded-full border-4 border-background bg-[#edf4fb] object-cover sm:h-24 sm:w-24" loading="lazy"/>:<div className="grid h-20 w-20 place-items-center rounded-full border-4 border-background bg-[#edf4fb] text-2xl font-black text-[#07315a] sm:h-24 sm:w-24">{String(p.name||"M").slice(0,2).toUpperCase()}</div>}<div className="pb-1"><h1 className="font-serif text-2xl sm:text-3xl">{p.name}</h1><p className="text-sm text-muted-foreground">@{p.username}</p></div></div><div className="flex flex-wrap gap-2">{!p.own&&<button disabled={follow.isPending} onClick={()=>follow.mutate(p.id)} className={`min-h-11 rounded-xl px-4 text-xs font-black ${p.followed?"border bg-background":"bg-[#07315a] text-white"}`}><UserPlus className="mr-2 inline h-4 w-4"/>{p.followed?"SEGUINDO":"SEGUIR"}</button>}<button onClick={share} className="min-h-11 rounded-xl border px-4 text-xs font-black"><Share2 className="mr-2 inline h-4 w-4"/>COMPARTILHAR PERFIL</button></div></div><div className="mt-5 flex flex-wrap gap-2 text-[10px] font-black"><span className="rounded-full bg-muted px-3 py-1">{String(p.role).toUpperCase()}</span>{p.profession&&<span className="rounded-full bg-[#f6efde] px-3 py-1 text-[#7b5a1d]">{p.profession}</span>}{(p.city||p.country)&&<span className="flex items-center gap-1 rounded-full bg-muted px-3 py-1"><MapPin className="h-3 w-3"/>{[p.city,p.country].filter(Boolean).join(", ")}</span>}</div>{p.bio&&<p className="mt-4 max-w-3xl whitespace-pre-wrap text-sm leading-6">{p.bio}</p>}<div className="mt-5 grid grid-cols-4 gap-2 sm:max-w-xl"><Metric value={p.postCount} label="Publicações"/><Metric value={p.articleCount} label="Artigos"/><Metric value={p.followers} label="Seguidores"/><Metric value={p.following} label="Seguindo"/></div>{!!p.interests?.length&&<div className="mt-5 flex flex-wrap gap-2">{p.interests.map((x:string)=><span key={x} className="rounded-full border px-3 py-1 text-[11px]">#{x.replace(/\s+/g,"")}</span>)}</div>}</div></section>
    {p.own&&<div className="grid gap-3 sm:grid-cols-2"><Link to="/cliente/rede-academica" search={{} as any} className="flex min-h-12 items-center gap-3 rounded-2xl border bg-card px-4 text-xs font-black"><BookOpen className="h-5 w-5 text-[#c99b38]"/>MEU DIÁRIO — PRIVADO</Link><Link to="/cliente/rede-academica" search={{} as any} className="flex min-h-12 items-center gap-3 rounded-2xl border bg-card px-4 text-xs font-black"><Bookmark className="h-5 w-5 text-[#07315a]"/>SALVOS</Link></div>}
    <nav className="grid grid-cols-4 overflow-hidden rounded-2xl border bg-card">{tabs.map(([k,l])=><button key={k} onClick={()=>setTab(k)} className={`min-h-12 px-2 text-[10px] font-black sm:text-xs ${tab===k?"bg-[#07315a] text-white":""}`}>{l}</button>)}</nav>
    {tab==="articles"?<div className="grid gap-4 sm:grid-cols-2">{data.articles.length?data.articles.map((a:any)=><Link key={a.id} to="/cliente/rede-academica/artigo/$slug" params={{slug:a.slug}} className="rounded-[24px] border bg-card p-5 shadow-sm"><span className="text-[10px] font-black text-[#9a772c]">{a.category||"ARTIGO"}</span><h2 className="mt-2 font-serif text-xl">{a.title}</h2><p className="mt-2 line-clamp-3 text-sm text-muted-foreground">{a.summary}</p></Link>):<Empty text="Nenhum artigo publicado ainda."/>}</div>:tab==="photos"?<Empty text="Fotos publicadas aparecerão aqui."/>:<div className="space-y-4">{posts.length?posts.map((x:any)=><article key={x.id} className="rounded-[24px] border bg-card p-5 shadow-sm"><div className="flex items-center justify-between gap-3"><span className="text-[10px] font-black text-[#9a772c]">{String(x.post_type).toUpperCase()}</span><span className="text-[10px] text-muted-foreground">{new Date(x.created_at).toLocaleDateString()}</span></div><p className="mt-3 whitespace-pre-wrap text-sm leading-6">{x.body}</p><div className="mt-4 border-t pt-3 text-xs text-muted-foreground"><HeartHandshake className="mr-2 inline h-4 w-4"/>Publicação acadêmica</div></article>):<Empty text="Nenhuma publicação nesta aba."/>}</div>}
  </div>
}
function Metric({value,label}:{value:number;label:string}){return <div className="rounded-2xl bg-muted/60 p-3 text-center"><b className="block text-lg">{value}</b><span className="text-[9px] font-bold text-muted-foreground sm:text-[10px]">{label}</span></div>}
function Empty({text}:{text:string}){return <div className="rounded-2xl border border-dashed p-8 text-center text-sm text-muted-foreground">{text}</div>}

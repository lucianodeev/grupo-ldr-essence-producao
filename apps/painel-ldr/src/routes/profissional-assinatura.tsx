import { Link, createFileRoute } from "@tanstack/react-router";
import { useServerFn } from "@tanstack/react-start";
import { useEffect, useState } from "react";
import { toast } from "sonner";
import { professionalCancelSubscription, professionalMySubscription } from "@/lib/professional-subscription.functions";

export const Route=createFileRoute("/profissional-assinatura")({head:()=>({meta:[{title:"Minha Assinatura — Rede LDR"},{name:"robots",content:"noindex,nofollow"}]}),component:SubscriptionPage});
function money(c:number,currency:string){return new Intl.NumberFormat(currency==="BRL"?"pt-BR":"pt-PT",{style:"currency",currency}).format((c||0)/100)}
function SubscriptionPage(){const load=useServerFn(professionalMySubscription);const cancel=useServerFn(professionalCancelSubscription);const [data,setData]=useState<any>(null);const [busy,setBusy]=useState(false);const refresh=async()=>setData(await load());useEffect(()=>{void refresh().catch(()=>window.location.replace("/profissional/login"))},[]);async function doCancel(){if(!window.confirm("Deseja cancelar a renovação automática ao fim do período atual? Seu histórico não será apagado."))return;setBusy(true);try{await cancel();toast.success("Cancelamento programado para o fim do período atual.");await refresh()}catch(e){toast.error(e instanceof Error?e.message:"Não foi possível atualizar a assinatura.")}finally{setBusy(false)}}if(!data)return <div className="min-h-screen grid place-items-center text-sm text-muted-foreground">Carregando assinatura…</div>;const s=data.subscription;const p=s?.subscription_plans;return <div className="min-h-screen bg-background"><header className="border-b bg-primary text-primary-foreground"><div className="mx-auto flex max-w-5xl flex-wrap items-center justify-between gap-3 px-4 py-5 sm:px-6"><div><p className="font-serif text-2xl">Minha Assinatura</p><p className="text-xs opacity-80">Rede de Profissionais LDR</p></div><Link to="/profissional-painel" className="rounded-xl border border-white/30 px-4 py-2 text-sm font-bold">Voltar ao painel</Link></div></header><main className="mx-auto max-w-3xl px-4 py-8 sm:px-6"><section className="rounded-3xl border bg-card p-6">{!s?<><h1 className="font-serif text-3xl">Nenhuma assinatura encontrada</h1><p className="mt-3 text-sm text-muted-foreground">Escolha um plano pelo painel profissional para concluir sua entrada na Rede.</p><Link to="/profissional-painel" className="mt-5 inline-block rounded-xl bg-primary px-5 py-3 font-black text-primary-foreground">ESCOLHER PLANO</Link></>:<><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[.16em] text-primary">PLANO ATUAL</p><h1 className="mt-1 font-serif text-3xl">{p?.name||"Plano Profissional"}</h1><p className="mt-2 text-2xl font-black">{p?`${money(p.amount_cents,p.currency)}/mês`:""}</p></div><span className="rounded-full bg-primary/10 px-3 py-1.5 text-xs font-black text-primary">{String(s.status).toUpperCase()}</span></div><div className="mt-6 grid gap-3 sm:grid-cols-2"><Info label="Início do período" value={s.current_period_start?new Date(s.current_period_start).toLocaleDateString():"—"}/><Info label="Fim do período" value={s.current_period_end?new Date(s.current_period_end).toLocaleDateString():"—"}/><Info label="Renovação" value={s.cancel_at_period_end?"Cancelamento programado":"Renovação automática"}/><Info label="Mercado" value={p?.market==="BR"?"Brasil":"Europa"}/></div><div className="mt-6 rounded-2xl bg-muted/50 p-4 text-sm leading-6"><strong>Importante:</strong> cancelar a renovação não apaga sua conta, histórico, pagamentos ou documentos. A publicação do perfil seguirá a regra da plataforma para assinaturas inativas após o término do período.</div>{s.status==="active"&&!s.cancel_at_period_end&&<button disabled={busy} onClick={doCancel} className="mt-6 rounded-xl border border-destructive px-5 py-3 font-bold text-destructive disabled:opacity-50">CANCELAR RENOVAÇÃO AO FIM DO PERÍODO</button>}{s.cancel_at_period_end&&<p className="mt-6 rounded-xl border p-4 text-sm font-semibold">Sua assinatura está programada para encerrar ao fim do período atual.</p>}</>}</section></main></div>}
function Info({label,value}:{label:string;value:string}){return <div className="rounded-2xl border p-4"><p className="text-xs font-black uppercase text-muted-foreground">{label}</p><p className="mt-2 font-bold">{value}</p></div>}

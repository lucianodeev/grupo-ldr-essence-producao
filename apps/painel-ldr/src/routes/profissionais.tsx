import { Link, createFileRoute } from "@tanstack/react-router";
import { useMemo, useState } from "react";
import { Globe2, Languages, MapPin, Monitor, Search, UserRound } from "lucide-react";
import { LanguageSelect, useI18n } from "@/lib/i18n";
import { networkLanding } from "@/lib/professional-network.functions";

export const Route = createFileRoute("/profissionais")({
  loader: () => networkLanding(),
  head: () => ({ meta: [{ title: "Encontre um Profissional — Rede LDR" }, { name: "description", content: "Encontre profissionais da Rede de Profissionais LDR por área, país, cidade, idioma e modalidade." }] }),
  component: Directory,
});

type AnyRow=Record<string,any>;
const COPY={
pt:{title:"ENCONTRE UM PROFISSIONAL",sub:"Pesquise profissionais com perfil ativo e aprovado na Rede LDR.",search:"Buscar por nome ou especialidade",area:"Área profissional",country:"País",language:"Idioma",modality:"Modalidade",all:"Todos",online:"Online",present:"Presencial",profile:"VER PERFIL",empty:"Nenhum profissional encontrado com estes filtros.",back:"Para profissionais"},
en:{title:"FIND A PROFESSIONAL",sub:"Search active and approved professionals in the LDR Network.",search:"Search by name or specialty",area:"Professional area",country:"Country",language:"Language",modality:"Modality",all:"All",online:"Online",present:"In person",profile:"VIEW PROFILE",empty:"No professionals match these filters.",back:"For professionals"},
fr:{title:"TROUVER UN PROFESSIONNEL",sub:"Recherchez les professionnels actifs et approuvés du Réseau LDR.",search:"Rechercher par nom ou spécialité",area:"Domaine professionnel",country:"Pays",language:"Langue",modality:"Modalité",all:"Tous",online:"En ligne",present:"Présentiel",profile:"VOIR LE PROFIL",empty:"Aucun professionnel ne correspond à ces filtres.",back:"Pour les professionnels"},
es:{title:"ENCUENTRA UN PROFESIONAL",sub:"Busca profesionales activos y aprobados de la Red LDR.",search:"Buscar por nombre o especialidad",area:"Área profesional",country:"País",language:"Idioma",modality:"Modalidad",all:"Todos",online:"Online",present:"Presencial",profile:"VER PERFIL",empty:"No hay profesionales con estos filtros.",back:"Para profesionales"}
} as const;

function Directory(){
 const {locale}=useI18n(); const c=COPY[locale]; const data=Route.useLoaderData() as any; const [q,setQ]=useState(""); const [cat,setCat]=useState(""); const [country,setCountry]=useState(""); const [lang,setLang]=useState(""); const [mode,setMode]=useState("");
 const categories=(data?.categories??[]) as AnyRow[]; const profiles=(data?.profiles??[]) as AnyRow[];
 const catMap=useMemo(()=>new Map(categories.map(x=>[x.id,x])),[categories]);
 const countries=[...new Set(profiles.map(p=>p.country_code).filter(Boolean))].sort(); const languages=[...new Set(profiles.flatMap(p=>p.languages??[]))].sort();
 const filtered=profiles.filter(p=>{const hay=`${p.display_name} ${p.professional_title} ${(p.specialties??[]).join(" ")} ${p.city??""}`.toLowerCase(); return (!q||hay.includes(q.toLowerCase()))&&(!cat||p.category_id===cat)&&(!country||p.country_code===country)&&(!lang||(p.languages??[]).includes(lang))&&(!mode||(mode==="online"?p.online_enabled:p.in_person_enabled));});
 return <div className="min-h-screen bg-background"><header className="sticky top-0 z-30 border-b bg-background/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between gap-3 px-4 py-4 sm:px-6"><Link to="/" className="font-serif text-xl font-bold">Grupo LDR Essence</Link><div className="flex items-center gap-2"><Link to="/para-profissionais" className="hidden text-sm font-bold sm:block">{c.back}</Link><LanguageSelect/></div></div></header><main className="mx-auto max-w-7xl px-4 py-10 sm:px-6"><div className="max-w-3xl"><p className="text-xs font-black uppercase tracking-[.18em] text-primary">Rede de Profissionais LDR</p><h1 className="mt-2 font-serif text-4xl sm:text-5xl">{c.title}</h1><p className="mt-3 text-muted-foreground">{c.sub}</p></div>
 <section className="mt-8 rounded-3xl border bg-card p-5"><div className="grid gap-3 lg:grid-cols-5"><label className="relative lg:col-span-2"><Search className="absolute left-3 top-3.5 h-4 w-4 text-muted-foreground"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder={c.search} className="w-full rounded-xl border bg-background py-3 pl-10 pr-3 text-sm"/></label><select value={cat} onChange={e=>setCat(e.target.value)} className="rounded-xl border bg-background px-3 py-3 text-sm"><option value="">{c.area}: {c.all}</option>{categories.map(x=><option key={x.id} value={x.id}>{x[`name_${locale}`]||x.name_pt}</option>)}</select><select value={country} onChange={e=>setCountry(e.target.value)} className="rounded-xl border bg-background px-3 py-3 text-sm"><option value="">{c.country}: {c.all}</option>{countries.map(x=><option key={x} value={x}>{x}</option>)}</select><select value={lang} onChange={e=>setLang(e.target.value)} className="rounded-xl border bg-background px-3 py-3 text-sm"><option value="">{c.language}: {c.all}</option>{languages.map(x=><option key={x} value={x}>{x.toUpperCase()}</option>)}</select></div><div className="mt-3 flex flex-wrap gap-2"><button onClick={()=>setMode("")} className={`rounded-full border px-4 py-2 text-sm font-bold ${!mode?"bg-primary text-primary-foreground":"bg-background"}`}>{c.all}</button><button onClick={()=>setMode("online")} className={`rounded-full border px-4 py-2 text-sm font-bold ${mode==="online"?"bg-primary text-primary-foreground":"bg-background"}`}>{c.online}</button><button onClick={()=>setMode("present")} className={`rounded-full border px-4 py-2 text-sm font-bold ${mode==="present"?"bg-primary text-primary-foreground":"bg-background"}`}>{c.present}</button></div></section>
 <section className="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{filtered.map(p=>{const category=catMap.get(p.category_id);return <article key={p.id} className="rounded-3xl border bg-card p-6 shadow-sm"><div className="flex items-start gap-4"><div className="grid h-16 w-16 shrink-0 place-items-center overflow-hidden rounded-2xl bg-primary/10">{p.photo_url?<img src={p.photo_url} alt="" className="h-full w-full object-cover"/>:<UserRound className="h-8 w-8 text-primary"/>}</div><div className="min-w-0"><h2 className="break-words font-serif text-2xl">{p.display_name}</h2><p className="mt-1 text-sm font-bold text-primary">{p.professional_title}</p></div></div><p className="mt-4 text-sm text-muted-foreground">{category?.[`name_${locale}`]||category?.name_pt}</p><div className="mt-4 space-y-2 text-sm"><p className="flex items-center gap-2"><MapPin className="h-4 w-4 text-primary"/>{p.city?`${p.city} · `:""}{p.country_code}</p><p className="flex items-center gap-2"><Languages className="h-4 w-4 text-primary"/>{(p.languages??[]).map((x:string)=>x.toUpperCase()).join(" · ")}</p></div><div className="mt-4 flex flex-wrap gap-2">{p.online_enabled?<span className="rounded-full bg-primary/10 px-3 py-1 text-xs font-black text-primary"><Monitor className="mr-1 inline h-3 w-3"/>{c.online.toUpperCase()}</span>:null}{p.in_person_enabled?<span className="rounded-full bg-secondary/20 px-3 py-1 text-xs font-black"><Globe2 className="mr-1 inline h-3 w-3"/>{c.present.toUpperCase()}</span>:null}</div><p className="mt-4 line-clamp-3 text-sm leading-6 text-muted-foreground">{p.about}</p><Link to="/profissional/$slug" params={{slug:p.slug}} className="mt-5 block rounded-xl bg-primary px-4 py-3 text-center text-sm font-black text-primary-foreground">{c.profile}</Link></article>})}</section>{filtered.length===0?<div className="mt-8 rounded-2xl border border-dashed p-8 text-center text-sm text-muted-foreground">{c.empty}</div>:null}
 </main></div>
}

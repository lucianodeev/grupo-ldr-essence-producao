export const PRESS_PUBLIC_REQUEST_LIMIT={windowMinutes:60,maxSubmissions:5} as const;export function normalizePressText(v:string,max=2000){return v.trim().replace(/[<>]/g,"").slice(0,max)}export function isValidPressEmail(v:string){return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v)&&v.length<=320}export const PRESS_EDITORIAL_LABELS={reporting:"Reportagem",opinion:"Opinião",institutional:"Institucional",release:"Release",sponsored:"Conteúdo patrocinado"} as const;
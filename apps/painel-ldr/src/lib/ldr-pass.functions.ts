import { createServerFn } from "@tanstack/react-start"; import { requireSupabaseAuth } from "@/integrations/supabase/auth-middleware";
function emailOf(c:Record<string,unknown>){return typeof c["email"]==="string"?c["email"] as string:null}
export const clientLdrPass=createServerFn({method:"GET"}).middleware([requireSupabaseAuth]).handler(async({context})=>{const {getLdrPassContext}=await import("@/lib/ldr-pass.server");return getLdrPassContext(context.userId,emailOf(context.claims))});
export const clientCreateLdrPassCheckout=createServerFn({method:"POST"}).middleware([requireSupabaseAuth]).inputValidator((d:{plan:"pass"|"pro"|"business";market:"BR"|"EU";billing:"monthly"|"annual";source?:string})=>d).handler(async({context,data})=>{if(!["pass","pro","business"].includes(data.plan)||!["BR","EU"].includes(data.market)||!["monthly","annual"].includes(data.billing))throw new Error("Plano inválido.");const {createLdrPassCheckout}=await import("@/lib/ldr-pass.server");return createLdrPassCheckout(context.userId,emailOf(context.claims),data.plan,data.market,data.billing,data.source??"academy")});

name: Fix library navigation
on:
  push:
    paths:
      - '.github/workflows/fix-library-navigation.yml'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test runner
        run: echo "library workflow runner ok"

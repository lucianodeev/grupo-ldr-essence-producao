name: Final training details and i18n (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: echo "Workflow legado desativado. A publicação atual usa o pipeline Publish card language sync."
